global:
  scrape_interval: 15s

scrape_configs:
  - job_name: rag-finland-backend
    metrics_path: /v1/metrics
    static_configs:
      - targets: ['backend:8000']
    authorization:
      type: Bearer
      credentials: replace-with-admin-api-key-or-jwt
